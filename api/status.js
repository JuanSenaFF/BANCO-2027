const {wrap,auth,github,fail}=require('../lib/server');
module.exports=wrap(['GET'],async(req,res)=>{await auth(req,true);const since=new URL(req.url,'https://internal.invalid').searchParams.get('since');if(!since||isNaN(Date.parse(since)))fail(400,'Data da solicitação inválida.');const data=await github('actions/workflows/update-vagas.yml/runs?event=workflow_dispatch&per_page=10');const run=data.workflow_runs.filter(r=>Date.parse(r.created_at)>=Date.parse(since)-5000).sort((a,b)=>a.id-b.id)[0];res.json(run?{status:run.status,conclusion:run.conclusion,id:run.id}:{status:'queued'});});
